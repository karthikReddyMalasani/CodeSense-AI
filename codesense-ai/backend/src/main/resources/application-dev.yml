spring:
  jpa:
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: false

logging:
  level:
    com.codesense: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
